public enum Enum_EnemyType
{
    GHOST,
    SKELETON,
    SPIDER
}

public enum Enum_EnemyRarityType
{
    A,
    B,
    C
}

public enum Enum_WaveStage
{
    BREAK,
    FIGHTING
}

public enum Enum_EXPType
{
    BLUE,
    YELLOW,
    RED
}